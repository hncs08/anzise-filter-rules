# AnziseFilter Rules

Public rule feed for AnziseFilter.

Raw update URL:

```text
https://raw.githubusercontent.com/hncs08/anzise-filter-rules/main/rules.json
```

Edit `rules.json`, commit, and push. Clients check for updates every 360 minutes by default.
